import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:dart_week_mobile/app/modules/movimentations/movimentations_module.dart';

void main() {
  initModule(MovimentationsModule());
  // MovimentationsController movimentations;
  //
  setUp(() {
    //     movimentations = MovimentationsModule.to.get<MovimentationsController>();
  });

  group('MovimentationsController Test', () {
    //   test("First Test", () {
    //     expect(movimentations, isInstanceOf<MovimentationsController>());
    //   });

    //   test("Set Value", () {
    //     expect(movimentations.value, equals(0));
    //     movimentations.increment();
    //     expect(movimentations.value, equals(1));
    //   });
  });
}
