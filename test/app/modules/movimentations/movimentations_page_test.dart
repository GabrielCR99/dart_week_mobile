import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('MovimentationsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MovimentationsPage(title: 'Movimentations')));
    //  final titleFinder = find.text('Movimentations');
    //  expect(titleFinder, findsOneWidget);
  });
}
